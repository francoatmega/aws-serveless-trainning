{
  "devDependencies": {
    "serverless-dotenv-plugin": "^4.0.2"
  },
  "scripts": {
    "deploy:dev": "sls deploy --stage dev",
    "deploy:hml": "sls deploy --stage hml",
    "deploy:prd": "sls deploy --stage prd",
    "invoke:local:dev": "sls invoke local -f function-scheduler --stage dev",
    "invoke:local:hml": "sls invoke local -f function-scheduler --stage hml",
    "invoke:local:prd": "sls invoke local -f function-scheduler --stage prd",
    "invoke:dev": "sls invoke -f function-scheduler --stage dev",
    "invoke:hml": "sls invoke -f function-scheduler --stage hml",
    "invoke:prd": "sls invoke -f function-scheduler --stage prd"
  },
  "dependencies": {
    "aws-sdk": "^2.1194.0",
    "axios": "^0.27.2",
    "cheerio": "^1.0.0-rc.12",
    "env-var": "^7.1.1"
  }
}
